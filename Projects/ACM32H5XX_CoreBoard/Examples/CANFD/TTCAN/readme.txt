Demo：	TTCAN示例
功能：	该mode用于基于时间触发的CAN帧的发送。
		需要连接CAN收发器(支持CAN FD)到总线上与另外一个或多个CAN FD节点相连。
		
		1、另一个节点通过发送ID为0x668的标准数据帧来同步该demo的时间基准，并触发该CAN节点的帧发送
		2、系统时钟（CAN模块时钟）配置成80MHz，仲裁段/数据段比特速率为250K/2M
		3、系统时钟需要使用PLL外部晶体的方式，因为CAN的时钟对精度要求比较高。