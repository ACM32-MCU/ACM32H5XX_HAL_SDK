Demo：	SPI Master示例
功能：	本示例含三个功能：
		TEST_MASTER_COMM： 使用Loop方式，作为SPI主机和另一方的SPI从机（使用SPI slave工程）进行数据收发通信，
		TEST_MASTER_DMA：    使用DMA方式，作为SPI主机和另一方的SPI从机（使用SPI slave工程）进行数据收发通信，
		TEST_MASTER_NOR_FLASH： 读写外部的SPI Nor Flash。 需要外接Nor Flash

		
		